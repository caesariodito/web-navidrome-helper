# Required: absolute path to Navidrome music directory
NAVIDROME_MUSIC_PATH=/srv/navidrome/music

# Optional: absolute path to nd-import binary (fallback: nd-import on PATH)
ND_IMPORT_BIN=/usr/local/bin/nd-import

# For docker-compose: host-side paths to mount into the container
HOST_NAVIDROME_MUSIC_PATH=/srv/navidrome/music
HOST_ND_IMPORT_BIN=/usr/local/bin/nd-import

# Optional: Pixeldrain API token for authenticated downloads
PIXELDRAIN_TOKEN=

# Optional: patterns for files to prune
UNNEEDED_FILES=

# Development helpers
MOCK_IMPORT=1
PORT=5000
